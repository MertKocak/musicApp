import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    input: {
        backgroundColor: "#FFDDBA",
        margin: 10,
        marginBottom: 0,
        height: 44,
        borderRadius: 12,
        fontSize: 18,
        alignContent: "center",
        padding: 10,
        color: "#FF5B1C",
        fontWeight: "bold"
      }
}
)